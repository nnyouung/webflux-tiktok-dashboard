spring:
  application:
    name: webflux-tiktok-dashboard

euler:
  api-key: ${EULER_API_KEY:test-key}
  ws-url: wss://ws.eulerstream.com

logging:
  level:
    com.webflux.connector.EulerConnector: DEBUG
